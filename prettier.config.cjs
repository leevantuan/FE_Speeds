module.exports = {
  printWidth: 100, // Giới hạn độ dài 1 dòng trước khi xuống dòng
  tabWidth: 2, // Tab = 2 spaces (chuẩn phổ biến của Angular)
  singleQuote: true, // Dùng '...' thay vì "..."
  semi: true, // Có dấu ; cuối câu
  trailingComma: 'all', // Dấu phẩy cuối object/array/function params
};
